# Copy to .streamlit/secrets.toml (local) or paste into the Streamlit Cloud
# "Secrets" box for your deployed app.

admin_password = "change-me-before-the-event"

# Optional: the public URL of the participant page, used for the share link
# and QR code shown on the host console.
# QUIZ_PUBLIC_URL is read from the environment, not secrets — set it in the
# app's "Advanced settings > environment" if your platform supports it.
