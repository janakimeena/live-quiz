# Copy to .streamlit/secrets.toml (local) or paste into the Streamlit Cloud
# "Secrets" box for your deployed app.

admin_password = "change-me-before-the-event"

# Optional: the app's public URL. When set, the host console shows a
# ready-to-share link and a QR code. Fill this in after the first deploy.
# public_url = "https://your-app.streamlit.app"
